port: 7880
rtc:
  tcp_port: 7881
  port_range_start: 50000
  port_range_end: 60000
  use_external_ip: true
keys:
  # Generate your own key/secret pair and mirror them into
  # LIVEKIT_API_KEY / LIVEKIT_API_SECRET in api/.env
  REPLACE_WITH_LIVEKIT_API_KEY: REPLACE_WITH_LIVEKIT_API_SECRET
logging:
  level: info
